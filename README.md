# Word2Vec
## Introduction
An Implementation of word2vec (CBOW) with negative sampling completely from scratch using numpy. For training I used Frankenstein; or, The Modern Prometheus by Mary Shelley (~78000 words).

## Usage
Make sure you have numpy installed, then modify the parameters in either of the run scripts (run.sh or run.bat) and run them.
